services:
  mysql:
    image: mysql:8.0.36
    container_name: mysql_cachefilmes
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
#  redis:
#    image: redis:7.2
#    container_name: redis_cachefilmes
#    restart: always
#    command: redis-server --appendonly yes
#    ports:
#      - "6379:6379"
#    volumes:
#      - redis_data:/data
#    networks:
#      - gym-stock-network
volumes:
  mysql_data:
#  redis_data: